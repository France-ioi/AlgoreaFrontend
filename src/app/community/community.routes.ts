import { Routes } from '@angular/router';
import { CommunityPageComponent } from './containers/community-page/community-page.component';

const routes: Routes = [
  {
    path: '',
    component: CommunityPageComponent,
  },
];

export default routes;
